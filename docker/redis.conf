# Bind tất cả interface (để host connect được)
bind 0.0.0.0

# Port
port 6379

# Enable persistence
appendonly yes

# Password (optional - bật nếu cần)
# requirepass yourpassword

# Memory policy (quan trọng cho production)
maxmemory 256mb
maxmemory-policy allkeys-lru

# Log
loglevel notice